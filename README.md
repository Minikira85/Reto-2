#ejercicio 1 

#Trabajamos únicamente con la muestra de entrenamiento, evitando en todo momento utilizar el conjunto de test, para no introducir sesgos ni información futura en el análisis.

#En lugar de asumir nombres teóricos de las variables, identificamos automáticamente las variables según su tipo (numéricas y categóricas). Esto hace el análisis más robusto y evita errores si los nombres del dataset cambian.

#Para las variables numéricas, realizamos:

#Un análisis tabular mediante estadísticos descriptivos básicos (mínimo, máximo, media, cuartiles).

#Un análisis gráfico mediante histogramas, lo que permite observar la forma de las distribuciones, posibles asimetrías y rangos de valores.

#Para las variables categóricas, realizamos:

#Tablas de frecuencias para conocer la distribución de las categorías.

#Diagramas de barras para facilitar la interpretación visual de dichas frecuencias.

#Todo el análisis se desarrolló en RMarkdown, usando código reproducible y sin referencias a rutas locales, garantizando que el documento pueda ejecutarse correctamente en cualquier entorno.

#Este análisis descriptivo permite detectar patrones, desequilibrios y características relevantes de las variables, y sirve como base para los contrastes bivariantes y los modelos de clasificación que se abordarán en las siguientes preguntas.
